apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rdvchart.fullname" . }}-test-connection"
  labels:
    {{- include "rdvchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rdvchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
